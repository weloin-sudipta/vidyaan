# Optimizations Memory

Performance / efficiency improvements discovered during tasks.

## Format

```yaml
id: opt_001
date: YYYY-MM-DD
type: optimization
context: "what was slow / wasteful"
improvement: "what made it faster"
gain: "rough estimate (e.g. 3x faster, 50% fewer tokens)"
tags: [nuxt, ssr, query]
```
